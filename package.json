{
  "name": "fitrip-website",
  "version": "1.0.0",
  "description": "Fitrip Fitness Club Website",
  "main": "index.html",
  "scripts": {
    "dev": "browser-sync start --server --files \"*.html, images/*, css/*.css, js/*.js\"",
    "start": "python -m http.server 8000"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}
